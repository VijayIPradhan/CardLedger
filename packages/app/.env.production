# Production API base URL — used by Vite builds (APK + prod web).
# The Docker web image overrides this with its own VITE_API_URL ("" → nginx /api proxy);
# this value is what the sideloaded Android APK talks to.
VITE_API_URL=https://cards.imvj.in/api
